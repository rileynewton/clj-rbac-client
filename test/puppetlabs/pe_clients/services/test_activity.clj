(ns puppetlabs.pe-clients.services.test-activity
  (:require [clojure.test :refer [deftest testing is]]
            [puppetlabs.pe-clients.services.activity :as activity]))

(deftest test-remote-activity-reporter
  (is true))
